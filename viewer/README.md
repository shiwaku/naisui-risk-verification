# 内水リスク検証ビューワ

標高（Mapterhorn）・水害履歴（浸水実績）・地形分類（国土地理院）を MapLibre GL JS で重ねる Web ビューワ。
[shiwaku/ksj-suigai-rireki-converter](https://github.com/shiwaku/ksj-suigai-rireki-converter) の
`viewer/`（浸水実績 + Mapterhorn 地形）を土台にし、地形分類のレイヤーを加えた。
浸水実績のタイル（PMTiles）とイベント索引（`public/events.json`）は同リポジトリの変換結果をそのまま使う。

公開先（予定）: <https://shiwaku.github.io/naisui-risk-verification/app/>

## できること

| 機能 | 内容 |
|---|---|
| 浸水域の表示 | 14,585 件 / 60 イベント（1896〜2019 年）。塗りと輪郭の2枚、不透明度スライダー付き |
| 成因 | **すべて / 台風 / 大雨・その他** のセグメント。塗り分けと絞り込みを兼ねる（台風 9,891 件 / 大雨・その他 4,694 件） |
| イベント単位 | 60 イベントを**検索して**1件に絞り、その範囲へ移動。選択は URL に載る |
| 地形分類（国土地理院） | 自然地形・人工地形を個別に表示。**内水関連のみ / すべて** のセグメントで絞り込み、凡例つき。クリックで土地の成り立ちと自然災害リスクを表示（浸水域と同じポップアップにまとめる）。ズーム 13 以上 |
| 地形（Mapterhorn） | **段彩**（標高レンジ選択・凡例つき）/ 陰影起伏（5方式・強調可変）/ 3D地形（起伏倍率可変）/ 等高線。段彩＋陰影起伏で陰影段彩図になる。**画面中央とクリック地点の DEM の細かさ**（1m / 5m / 10m メッシュ）を表示 |
| 背景地図 | 淡色 / 標準（地理院 最適化ベクトルタイル）/ 写真（地理院シームレス空中写真）/ 白図 |
| その他 | ライト/ダークテーマ、クリックで属性ポップアップ（重なった浸水域を全件）、位置を URL ハッシュに保存 |

## 特定の災害だけを見る / 共有する

「水害イベント」の入力欄で災害名・年・元号・ファイル名を検索して1件に絞る。
全角括弧「（平成12）」と半角括弧「(平成8)」、全角数字が元データで混在しているため、
検索は NFKC 正規化して照合する。空白区切りは AND。

```
東海        → 2000（平成12）年 9月 台風14号・東海豪雨
2000        → 同じ
平成12 / h12 → 同じ
伊勢湾      → 1959（昭和34）年9月 伊勢湾台風
s34         → 1959年8月・9月の2件
```

選択したイベントは `?event=<元Shapefile名>` として URL に載るので、そのままリンクを渡せる。

```
# 2000年の東海豪雨だけを表示（範囲へ自動で寄る）
https://shiwaku.github.io/ksj-suigai-rireki-converter/app/?event=2000_09_h12_sinsui_t_add22.shp

# 位置も指定する（#ズーム/緯度/経度 がある場合はそちらを尊重し、自動ズームしない）
.../app/?event=2000_09_h12_sinsui_t_add22.shp#11/35.18/136.90
```

`?event=` に知らない値が来たらクエリごと捨てて全イベント表示に戻る。
地図位置は MapLibre が URL のハッシュに書くため、`?event=` はクエリ側だけを
`history.replaceState` で書き換える（絞り込み操作で戻るボタンの行き先を増やさない）。

## 使い方

```sh
cd viewer
npm install
npm run dev      # http://localhost:5175
npm run build    # 型チェック → 各種検証 → スタイル書き出し → ../app/ へビルド
npm run preview  # ビルド結果を確認
```

`npm run build` が唯一のゲートで、以下を順に通す。

1. `tsc --noEmit` — 型チェック
2. `npm run check:style` — テーマ × 成因 × イベント指定 × 地形の全組み合わせを MapLibre
   スタイル仕様に照らして検証し、あわせて成因の判定式を実際に評価して 11 パターンの
   期待値と照合する（`isTyphoonExpr` と `build_events.py` の `is_typhoon()` が
   食い違わないようにするため）。式の書き間違いはブラウザで該当の組み合わせを
   開くまで気付けないので総当たりで通す
3. `npm run check:search` — イベント検索の絞り込みを `public/events.json` の実データで検証する。
   括弧や数字の全角半角の正規化を間違えると「東海」や「2000」で目的の災害に当たらなくなるが、
   ブラウザで打ってみるまで気付けないため、代表的なクエリの期待結果を照合する
4. `npm run check:relief` — 段彩の標高→色の引き当てを検証する。terrarium のバイト並びの
   解釈やルックアップテーブルの添字を間違えても例外は出ず「なんとなく変な色の地図」に
   なるだけなので、代表的な標高の色・**微小な標高差（2cm〜34cm）が色の差になること**・
   レンジを狭めた効果の倍率・隣の段が見分けられること・凡例と地図の色の一致・
   DEM タイルの到達性を確かめる。標高を丸める実装に戻ると微地形の項目が落ちる
5. `npm run check:stacking` — 自前レイヤーの差し込み位置が背景地図の建築物・道路・
   構造物より上、注記より下であることを実際のスタイルで検証する。ここがずれても
   例外は出ず「なんとなく見えにくい地図」になるだけで気付けない
6. `npm run export:style` — `public/style/sinsui-{light,dark}.json` を書き出す
7. `vite build` — `../app/` へ出力

### 配信先の差し替え

浸水実績 PMTiles の既定は GitHub Pages 上の変換結果。焼き直したタイルを push 前に見る場合:

```sh
VITE_PMTILES_URL=http://localhost:8080/sinsui_all.pmtiles npm run dev
```

### イベント索引の再生成

イベント名（`disastName`）と各イベントの範囲はタイルの見えている部分からしか引けないため、
変換結果から静的な索引 `public/events.json` を作って使っている。データを差し替えたら再生成する:

```sh
uv run python viewer/scripts/build_events.py   # ksj-suigai-rireki-converter のルートで
```

索引は浸水実績の変換結果（`output/sinsui_all.gpkg`）から作るため、変換元のリポジトリで
再生成してコピーする。

### 地形分類のコード表の再生成

地形分類の GeoJSON タイルは属性が `code` だけで、分類名・色・説明は地理院の `style.js` にしかない。
これを `src/landform-codes.json` に焼いて使っている。地理院が分類を更新したら作り直す:

```sh
node scripts/build_landform_codes.mjs   # viewer/ で
```

## 構成

```
viewer/
  index.html            パネルの静的マークアップ
  src/
    main.ts             状態 → レイヤーの組み立て、UI の配線
    layers.ts           浸水実績のソース・レイヤー・配色・絞り込み・ポップアップ
    terrain.ts          Mapterhorn の DEM（陰影起伏 / 3D地形 / 等高線）
    relief.ts           段彩（DEM をピクセル単位で色に置き換える relief:// プロトコル）
    coverage.ts         DEM の被覆（地点ごとにどの細かさの DEM が使われているか）
    landform.ts         地形分類（GeoJSON タイル → MVT の landform:// プロトコル・レイヤー・凡例）
    landform-codes.json 地形分類のコード表（生成物・コミット対象）
    basemap.ts          背景地図の切替とダーク化
    events.ts           イベント索引（events.json）の読み込みと ?event= の読み書き
    eventPicker.ts      イベントの検索付き選択（コンボボックス）
    theme.ts            ライト/ダークの保存と反映
    style.css           デザイントークンとパネル
  scripts/
    check-style.mjs     全組み合わせのスタイル検証
    check-search.mjs    イベント検索の絞り込み検証
    check-relief.mjs    段彩の標高→色の検証
    check-stacking.mjs  自前レイヤーの差し込み位置の検証
    export-style.mjs    静的スタイルの書き出し（QGIS / Maputnik 用）
    build_events.py     events.json の生成（geopandas）
    build_landform_codes.mjs  landform-codes.json の生成（地理院の style.js から）
  public/
    pale.json std.json  地理院 最適化ベクトルタイルのスタイル（dm-converter から取得）
    events.json         イベント索引（生成物・コミット対象）
    style/              書き出したスタイル（生成物・コミット対象）
```

## 設計上の判断

### ビルド成果物を `app/` に置いている

GitHub Pages で `main` ブランチのルートを配信し、ビューワはリポジトリ直下の `app/` に出して
コミットする（`vite.config.ts` の `build.outDir`）。今後の解析結果（`output/` の PMTiles など）も
同じ Pages から配れるようにするため、`gh-pages` ブランチには分けない。
`base` は相対パス（`./`）にしてあり、配信先のパスに依存しない。

### 自前レイヤーは背景地図の注記の下に差し込む

積み順は下から:

```
背景地図（水域・河川・道路・建築物・構造物すべて）
  → 段彩
  → 地形分類（自然地形 → 人工地形）
  → 陰影起伏
  → 浸水域（塗り・輪郭）
  → 等高線
背景地図の注記（地名・河川名）
```

地形分類は段彩と同じく陰影起伏の下に置く。分類の色に陰影が乗り、分類と起伏を一度に読める。
人工地形（盛土地・干拓地など）は自然地形の上に重ねる（地理院地図と同じ順）。

注記だけを上に残す。浸水域を最前面に置くと「どの川・どの町が浸水したか」が読めない。
段彩を陰影起伏の下に置くと、陰影が段彩に乗って陰影段彩図になる。等高線を浸水域の上に
置くのは、浸水域を透かして地形の高低を追えるようにするため。

差し込み位置は **`source-layer` が `Anno` のレイヤー**を目印に探す（`labelBeforeId()`）。
当初は「最初の symbol レイヤー」を注記とみなしていたが、地理院 最適化ベクトルタイルの
最初の symbol は水部の小さな注記（`水部表記線point`、123レイヤー中の13番目）で、
実際の注記は末尾の 114〜122 番だった。そのため自前のレイヤーが**背景地図の101レイヤーの
下**に埋まり、市街地では建築物の不透明な塗り（36〜108番）に段彩も浸水域も潰れていた。
例外は出ず「なんとなく見えにくい地図」になるだけで気付けないので、`check:stacking` で
差し込み位置が建築物・道路・構造物より上、注記より下であることを検証している。

なお「段彩を背景地図の線画より下に敷いて河川や道路を鮮明に残す」案は採らなかった。
市街地では建築物の不透明な塗りに段彩が埋まり、微小な窪地を読む目的と両立しない。
段彩は既定 55% なので河川・道路は透けて見える。

### 地形分類（国土地理院 ベクトルタイル提供実験）

#### GeoJSON タイルをその場で MVT に詰め替える

地形分類は GeoJSON タイル（`experimental_landformclassification{1,2}/{z}/{x}/{y}.geojson`）で
配信されている。MapLibre の GeoJSON ソースは1ファイル丸ごとしか扱えず、タイルとしては読めない。
そこで `landform://<natural|artificial>/{z}/{x}/{y}` プロトコルでタイルを取得し、
[geojson-vt](https://github.com/mapbox/geojson-vt) で切って [vt-pbf](https://github.com/mapbox/vt-pbf)
で MVT に詰め、vector ソースとして渡す（`src/landform.ts`）。事前にタイルを焼き直さないので、
地理院のデータ更新がそのまま反映される。データの無い区域は 404 が返るので空タイルとして扱う。

分類名・内水フラグ・色はプロトコルの中で属性に焼き込む。スタイル側に 82 コードの `match` を
書くと、凡例（`landform-codes.json`）と地図の色の対応が二重管理になるため。

#### 地理院からは z14 のタイルだけを読み、z13 以上で表示する

地理院から取得するのは z14 のタイルだけ。

- 人工地形は z4〜13 が「おおよその範囲図」で実データではない
- 自然地形の z13 以下は広域版で、分類の体系（コード 1〜71）が詳細版と違う
- z14 と z16 の形状は同じ。実測（2026-09、`14/14555/6446`）で、z14 の1枚と、その子の z16
  16枚の頂点数の差は約 7%（自然 3,535 → 3,796、人工 891 → 1,045）で、タイル境界で切れた分に
  相当する。z15 以上は z14 を拡大表示すれば足りる

ソースは `minzoom = 13` / `maxzoom = 14`。z13 のタイルを要求されたら、子にあたる z14 の
4枚を取得してまとめ、z13 のタイルとして返す（z13 の広さでも詳細版の分類が見える）。
子タイルの境界で地物は切れたまま並ぶが、輪郭を描かない塗りだけなので継ぎ目は見えない。
取得枚数はズームを1段下げるごとに4倍になる。画面1枚あたり（種別ごと）z14 で約12枚、
z13 で約48枚、z12 まで広げると約190枚（z14 は1枚最大 360KB 程度）になり、地理院の
サーバにも重いので z13 で止めた。

z13 未満では表示されないので、パネルに「ズーム 13 以上で表示」と現在のズームを出す。
詳細版は全国を覆っていないため、z13 以上でも何も出ない地域がある。

#### 「内水関連のみ」を既定にする

表示の既定は内水に関係が深い分類だけ（調査メモ §3.2）:

| 区分 | 分類 |
|---|---|
| 自然地形 | 凹地・浅い谷 / 後背低地・湿地 / 旧河道 / 落堀 / 旧水部 |
| 人工地形 | 干拓地 / 盛土地・埋立地 |

全22分類を塗ると、山地・台地・氾濫平野などの広い面が画面を埋め、浸水域との対応が読めなくなる。
盛土地・埋立地は「盛土で窪地が消えていても元の地形がリスクを持つ可能性」があるので含めた。
この一覧は `scripts/build_landform_codes.mjs` の `NAISUI` にあり、分類名が `style.js` から
消えたら生成が止まる。

色は地理院のスタイル（`style.js`）のまま。地理院地図で見慣れた配色と対照できることを優先した。
輪郭は付けない（隣り合う分類の境界は色の差で読めるうえ、浸水域の輪郭と紛れる）。

#### ホバーのカーソル判定には入れない

地形分類は画面を隙間なく覆うので、カーソル判定に入れるとどこでも指カーソルになり、
浸水域の上かどうかが分からなくなる。クリック時だけ問い合わせ、浸水域と同じポップアップの
先頭に「地形分類」の節を出す（地点の成り立ちを読んでから、そこで起きた浸水を読む順）。

### 地図に持たせる区別は「成因」だけにした

当初は年代を5階級の濃淡で塗っていたが、浸水域は年をまたいで何枚も重なるため、
重なった部分が「どの年代でもない濁った色」になって階級の意味が消える。
単一イベントを選ぶと凡例のほとんどが 0 件で並ぶ問題もあった。
年の絞り込み（期間スライダー）もイベント検索と役割が重複していた。

代わりに、水害の読み方として意味のある「台風性か、大雨・その他か」の2値だけを
色に持たせ、同じセグメントで絞り込みも兼ねる。個々の年月はクリックした
ポップアップで読める。

色は判別が検証済みのカテゴリ配色の1・2枠（オレンジ `#eb6834` / 青 `#2a78d6`、
ダークは `#d95926` / `#3987e5`）。全ペアの色覚特性シミュレーション下の分離
（最悪 ΔE 24.7 / 26.8）と地図面に対する 3:1 以上のコントラストを満たす。
片方だけを表示しても色の意味は変えない（色は対象に従い、表示状態に従わない）。

### 成因の判定規則

災害名（`disastName`）に「台風」を含むものを台風性とし、名称が無い 37 件は
元ファイル名の `_t` サフィックス（`typhoon_file`）で補う。

ファイル名のフラグだけで判定すると、1ファイルに「大雨」と「台風第6号」が混在する
`1961_06_s36` を取り違える（17 件）。災害名はレコード単位なのでそこまで分かれる。
七夕豪雨・東海豪雨のように台風由来の豪雨は台風に入る。

この規則は `scripts/build_events.py` の `is_typhoon()`（凡例の件数）と
`src/layers.ts` の `isTyphoonExpr`（地図の色と絞り込み）に二重で実装されている。
**片方だけ変えると地図と凡例が食い違う**ため、`check:style` が MapLibre の式を
実際に評価して規則を固定している。

### 段彩（陰影段彩図）

MapLibre には標高を直接色に写すレイヤーが無い。DEM タイルを取得してピクセルごとに
標高を読み、色に置き換えたラスタタイルを返すカスタムプロトコル（`relief://`）で作る。
方式は国土地理院の点群タイル閲覧サイト
（[gsi-cyberjapan/3dpc-3dtiles](https://github.com/gsi-cyberjapan/3dpc-3dtiles)）に倣った。
ピクセル走査の実装は全国Ｑ地図（MIT license, Copyright 2024 全国Ｑ地図管理者）に由来し、
`src/relief.ts` の冒頭に表示を残している。

**段彩は陰影起伏の下に敷く**のが要点。陰影が段彩の上に乗ることで陰影段彩図になる
（積み順は `beforeIdFor()`）。

配色は点群タイル閲覧サイトの既定値（全国Ｑ地図由来）で、日本の地形図で見慣れた
「低地は青緑 → 平野は緑 → 山地は黄〜茶 → 高山は白」。
**海面下も塗る**（標高0以下を透明にすると、いちばん見たい海抜0m地帯が消える）。

#### 標高レンジ — 微小な窪地を読むために

「全国」の配色は -10〜4000m を16段で塗る。山地を含む広域では正しいが、
**低地の内水浸水の原因を読むには使えない**。標高 0〜10m の氾濫平野は2段に収まって
しまい、内水を溜める微小な窪地（数十cm〜数mの凹み）が現れない。

そこでレンジを選べるようにし、指定レンジでは **1段の刻み幅**を決めて全国の配色を
段数ぶんに補間して塗る。

| レンジ | 刻み | 段数 |
|---|---|---|
| 全国（絶対標高 -10〜4000m） | 帯ごとに 1m〜1000m | 16 |
| 山地 0〜1000m | 50m | 20 |
| 平野 0〜100m | 5m | 20 |
| 低地 0〜20m（既定） | 1m | 20 |
| 微地形 0〜5m（窪地） | 0.5m | 10 |

当初は 15 色を min〜max に等分していたが、1段が「0〜5m で 0.357m」「0〜20m で 1.43m」と
半端になり、凡例の目盛りも読みにくかった。刻みを先に決めれば目盛りが 0, 0.5, 1.0… と
揃い、「この窪地は1段 = 50cm 低い」と読める。0.5m は基盤地図情報 DEM の標高精度
（±0.3m 程度）にも見合う。段の中は線形補間するので、刻みより小さい差も色に出る。

同じ 34cm の標高差に対する色の差は、全国レンジで 4/255、微地形レンジで 74/255（19倍）。
`check:relief` がこの比と、段の境界が刻みに乗っていること、凡例の数字が端に付くことを
検証している。

補間の元は**先頭の色を1つ落とした15色**。`TINTS` の -10m と 0m は
意図的に同色（海面下と海面を同じ色で塗る）で、そのまま使うと最下段の2段が
同色になり、レンジの下端——低地でいちばん見たい足元——が1段ぶん潰れてしまう。
20段のレンジでは隣の段の色差が元の 14/20 に縮む（最小 19.6/255、白系）が見分けられる。

レンジは**タイルURLに埋め込む**（`relief://<mode>/<min>/<max>/<step>/<DEM URL>`）。
MapLibre はタイルをURL単位でキャッシュするため、モジュール変数で持つと
レンジを変えても古い色のタイルが残る。

DEM の細かさは深いズームにしか現れないので、段彩ソースの `maxzoom` は 15。
`raster-resampling: nearest` にしてあるのは、補間で隣接ピクセルが混ざると
窪地の縁と1段の差がぼやけるため。

#### 標高から色を引くのはルックアップテーブル

ピクセルごとに色の帯を線形探索すると、1タイル 512×512 = 26万回の分岐と乗算が走る。
テーブルなら「配列3回読み」で済む。

段数はレンジの幅から決め、**どのレンジでも標高 1cm 刻み**にする
（全国レンジは上限 40万段 = 1.2MB）。ここは2回間違えた。

1. 当初は terrarium の上位2バイト `(r<<8)|g` をそのまま添字にした。実装は単純だが
   これは**標高を 1m に丸める**ことになり、微小な窪地が塗り潰される
2. 次に固定 4096 段にしたが、全国レンジでは1段が約1mになり、0〜1m や 1〜10m といった
   細い帯の境界を踏み外して凡例と地図の色が食い違った（`check:relief` が検出）

`b`（1/256m）まで含めて標高を復元し、レンジ幅に応じた段数で引く。1cm は基盤地図情報
DEM の標高精度（DEM5A / DEM1A で ±0.3m 程度）より十分細かい。

#### 凡例

帯は**等幅**で並べる。「全国」レンジでは実際の標高間隔が 1m〜1000m と幅が違い、
値に比例した幅にすると低標高側（0〜140m）が全体の数%に潰れて読めない。
指定レンジでは刻み幅で等間隔に割っているので、等幅がそのまま実際の間隔になる。
どちらでも実際の境界は目盛りの数字が示す。数字は段数を割り切る間隔で間引き、端の
min と max には必ず付ける（0〜20m なら 0, 5, 10, 15, 20）。

### DEM の細かさを地点ごとに伝える

段彩を「微地形 0〜5m」まで狭めても、実際に窪地が出るかは**その地域の DEM の細かさ**で
決まる。Mapterhorn が日本について持つのは基盤地図情報 DEM の 1m（部分的）/ 5m（平野部
中心）/ 10m（全国）で、10m メッシュの地域では 10m 格子より細かい凹凸は元データに無い。
画面上は同じように色が塗られるので、「窪地が無い」のか「DEM が粗くて出ていない」のかが
利用者には区別できなかった。

地形タイル自体（terrarium）には出典が入っていないため、Mapterhorn の
[Coverage ページ](https://mapterhorn.com/coverage)が使っている被覆ベクトルタイルを読む。

```
TileJSON:      https://single-archive-tiles.mapterhorn.com/coverage.json
source-layer:  coverage（z0〜14）、属性 source = jpdem1a / jpdem5a / … / glo30
```

各ソースの GeoTIFF を実データ範囲でポリゴン化したもので、Mapterhorn の合成規則は
「細かいソース優先で、穴を次に細かいソースで埋める」。したがって**地点に重なるソースの
うち最も細かいものが、その地点で使われている DEM** になる（`src/coverage.ts` の
`finestDem()`）。これを透明な塗り（`fill-opacity: 0`）で常に載せ、`queryRenderedFeatures`
で問い合わせる。`visibility: none` では問い合わせの対象から外れるので不透明度で隠す。

表示は 2 箇所。段彩パネルの凡例の下に**画面中央の DEM**を idle ごとに更新して出し
（「1m メッシュ（基盤地図情報 DEM1A）。数十cm〜数mの窪地まで読める」）、浸水域を
クリックしたポップアップの末尾に**その地点の DEM**を出す。

限界: 被覆ポリゴンは z14 で約 0.375px（数 m）に単純化されており、ソースの継ぎ目の
数ピクセルはガウスぼかしで両方が混ざる。境界付近では厳密ではない。
実測（2026-09、z14 タイルをデコード）: 江東・能登・北アルプス・人吉には `jpdem1a`、
十勝は `jpdem5a` まで。どこにも `jpdem10b` と `glo30` が敷かれている。

調査の全文は [`docs/mapterhorn-dem-sources.md`](../docs/mapterhorn-dem-sources.md)。
被覆タイルを使わずに配信される最大ズームで判定する手もある（1m の地域は z16 まで、
5m は z14 までしか配信されない）が、粒度が z12 マクロタイル（約 10km 角）なので、
1m と 5m が混在する区画を区別できず採らなかった。

### DEM の配信方式は選ばせない

Mapterhorn は TileJSON / ZXY / PMTiles の3通りで DEM を配信しており、
上流の mapterhorn-viewer はこれを画面から選べるようにしている。ここでは撤去した。
見た目はどれも同じで、PMTiles 方式は 705GB のアーカイブへの範囲読みで明確に遅い。
浸水履歴を読むうえで選ぶ意味がなく、操作を迷わせるだけだった。
既定の TileJSON に固定し、切り分けが必要なときだけ `?dem=zxy` / `?dem=pmtiles`
で差し替える。

maplibre-contour の `DemSource` は DEM を自前の HTTP で取るため MapLibre の
プロトコルを経由できず、等高線は常に ZXY から生成する。

### 3D地形と併用しても止まらないようにする

浸水域は 14,585 件の大きなポリゴンで、広域ではタイル1枚が 500KB 近くある。
3D地形はこれと相性が悪く、素朴に組むと操作中に描画が止まる。効いた対策:

| 対策 | 理由 |
|---|---|
| `maxPitch` を 70 に制限 | 視線を倒すほど地平線の先まで表示範囲に入り、要求タイルが数百枚に膨らむ |
| `maxTileCacheSize` を 96（モバイル 24）に明示 | 既定の 512 枚だと 500KB 級のタイルを抱えてメモリを使い切り、GC で止まる |
| 地形の有効化とカメラの傾けを直列化 | メッシュ生成・DEM取得・アニメーションが重なるとフレームが落ちる |
| 等高線を上流サンプルの範囲に戻す | z15 で 10m 刻みにすると平野で線が延々と蛇行し、セグメント数が跳ね上がる。線は z10 以上、標高の数字は z12 以上に制限 |
| 輪郭線を z9 以上に | 広域では 0.4px の毛になって境界を示さないのに、14,585 件ぶんの線を組み立てるコストだけかかる |

UI 側では、操作の重さに応じて処理を分けている。`setFilter` は該当レイヤーの
読み込み済みタイルを作り直させるため、絞り込みが変わったときだけ呼ぶ
（`applyFilter` / `applyVisibility` / `applyOpacity`）。不透明度スライダーで
これを呼ぶと1目盛りごとに全タイルが再構築され、ドラッグ中に固まる。
`mousemove` の地物取得と 3D地形の起伏倍率も1フレーム1回に束ねている。
レイヤーはグループごとに積み直し（`applyHillshade` / `applySinsuiLayers` /
`applyContours`）、地形を切り替えても浸水域には触らない。

### 不透明度の既定は 0.35

浸水域は同じ場所で重なる（実測で中央値2枚・最大6枚）。同一レイヤー内の重なりは
アルファが積算されるため、合成後の不透明度は `1-(1-a)^n` になる。
`a=0.6` では2枚で 0.84、3枚で 0.94 に達し、そこから 1.0 まで上げても見た目が
変わらない（スライダーの上半分が死ぬ）。積算後にちょうど背景が透ける 0.35 を既定にした。

## 出典

- 浸水実績: [国土数値情報（水害履歴・浸水実績）国土交通省](https://nlftp.mlit.go.jp/ksj/) を加工して作成
- 地形分類: [国土地理院 ベクトルタイル提供実験（地形分類）](https://github.com/gsi-cyberjapan/experimental_landformclassification)。提供実験であり基本測量成果ではない。表示されるリスクは分類ごとの一般的な傾向
- 地形: [Mapterhorn](https://mapterhorn.com/)（[attribution](https://mapterhorn.com/attribution)）
- 段彩の実装: [gsi-cyberjapan/3dpc-3dtiles](https://github.com/gsi-cyberjapan/3dpc-3dtiles)（国土地理院 点群タイル閲覧サイト）を参考。ピクセル走査は[全国Ｑ地図](https://github.com/qchizu/qchizu_maplibre)（MIT license, Copyright 2024 全国Ｑ地図管理者）由来
- Mapterhorn は日本について国土地理院の基盤地図情報 DEM（DEM1A=1mメッシュ、DEM5A/5B/5C=5mメッシュ、DEM10A/10B=10mメッシュ）を含んでいる（[attribution.json](https://download.mapterhorn.com/attribution.json) の `jpdem*`）。段彩・陰影起伏・等高線・3D地形はすべてこれを読んでいる
- 背景: [国土地理院 最適化ベクトルタイル](https://github.com/gsi-cyberjapan/optimal_bvmap) / [地理院タイル](https://maps.gsi.go.jp/development/ichiran.html)
